name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --check

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get install -y libxkbcommon-dev libwayland-dev libxrandr-dev libxi-dev libxcursor-dev libx11-dev pkg-config
      - run: cargo build

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get install -y libxkbcommon-dev libwayland-dev libxrandr-dev libxi-dev libxcursor-dev libx11-dev pkg-config
      - run: cargo test

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get install -y libxkbcommon-dev libwayland-dev libxrandr-dev libxi-dev libxcursor-dev libx11-dev pkg-config
      - run: cargo clippy -- -D warnings

  ci:
    name: CI
    runs-on: ubuntu-latest
    needs: [fmt, build, test, clippy]
    steps:
      - run: echo "All checks passed"
